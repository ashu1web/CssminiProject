/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      './dist/**/*.html', // Correct path for HTML files in the dist directory
    ],

  theme: {
    extend: {},
  },
  plugins: [],
}
